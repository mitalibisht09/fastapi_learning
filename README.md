## 📝 My FastAPI Learning Log

* **Day 1:** FastAPI environment setup & basic GET routes (`/`, `/student`, `/users`, `/profile`).
* *(More updates as I learn every day!)*
* **Day 2:** Learned dynamic path parameters (`/users/{user_id}`) and query parameters (`/users?name=...`) with optional defaults using `str = None`.
